{
  "name": "orchestrate",
  "version": "0.1.0",
  "description": "CLI to coordinate coding agents via git worktrees",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "orchestrate": "./bin/run.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node src/index.ts",
    "test": "echo \"no tests yet\""
  },
  "dependencies": {
    "@oclif/core": "^3.21.0",
    "execa": "^5.1.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "oclif": {
    "bin": "orchestrate",
    "commands": "./dist/commands"
  }
}
